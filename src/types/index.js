// Type definitions converted to JSDoc comments for JavaScript

/**
 * @typedef {'fixed' | 'usage' | 'credit' | 'license' | 'entitlement'} ChargeType
 */

/**
 * @typedef {'standard' | 'tiered' | 'volume' | 'package' | 'percentage'} PricingModel
 */

// Export empty object to make this a valid ES module
// Types are now documented via JSDoc comments above
export { };
